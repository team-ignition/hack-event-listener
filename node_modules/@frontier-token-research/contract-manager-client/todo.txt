Next steps:

	- create a script that uses the sem release exported version
	- create a script which updates the contract manager using the functions defined in trl/scripts/updateContractManager
	- convert that script into a cli
	- add functions to client.js which allow for basic queries of the API
	- add documentation
	- add unit tests
